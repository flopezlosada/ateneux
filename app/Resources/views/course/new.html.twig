{% extends '::layout_crud.html.twig' %}
{% block title_wrapper %}Añadir curso{% endblock %}
{% block main %}
    <div class="panel panel-default">

        <div class="panel-body">
            {{ form_start(form, {'attr': {'role': 'form' }}) }}
            <div class="row">
                <div class="col-lg-6">

                    <div class="form-group">
                        {{ form_errors(form.title) }}
                        <label>{{ form_label(form.title) }}</label>
                        {{ form_widget(form.title,{'attr':{'class':'form-control','placeholder':'Nombre del curso'}}) }}
                    </div>
                    <div class="form-group">
                        {{ form_errors(form.course_type) }}
                        <label>{{ form_label(form.course_type) }}</label>
                        {{ form_widget(form.course_type,{'attr':{'class':'form-control','placeholder':'Tipo de curso'}}) }}
                    </div>
                    <div class="form-group">
                        {{ form_errors(form.start_date) }}
                        <label>{{ form_label(form.start_date) }}</label>
                        {{ form_widget(form.start_date,{'attr':{'placeholder':'Fecha de inicio'}}) }}
                    </div>
                    <div class="form-group">
                        {{ form_errors(form.end_date) }}
                        <label>{{ form_label(form.end_date) }}</label>
                        {{ form_widget(form.end_date,{'attr':{'placeholder':'Fecha de finalización'}}) }}
                    </div>
                    <div class="form-group">
                        {{ form_errors(form.mentor_teacher) }}
                        <label>{{ form_label(form.mentor_teacher) }}</label>
                        {{ form_widget(form.mentor_teacher,{'attr':{'class':'form-control','placeholder':'Tutora o tutor'}}) }}
                    </div>
                </div>
            </div>
            <input type="submit" value="Create"/>
            {{ form_end(form) }}
        </div>
    </div>



    <ul>
        <li>
            <a href="{{ path('course_index') }}">{{"Back to the list"|trans}}</a>
        </li>
    </ul>
{% endblock %}
{% block specific_js %}
    <script>
        $('.datepicker').pickadate({
            formatSubmit: 'yyyy-mm-dd',
            onStart: function () {
                {% if course.getStartDate() %}
                var date = new Date("{{  course.getStartDate()}}");
                this.set('select', date)
                {% endif %}
            }
        })
        $('.datepicker2').pickadate({
            formatSubmit: 'yyyy-mm-dd',
            onStart: function () {
                {% if course.getEndDate() %}
                var date = new Date("{{  course.getEndDate()}}");
                this.set('select', date)
                {% endif %}
            }
        })
    </script>
{% endblock %}

