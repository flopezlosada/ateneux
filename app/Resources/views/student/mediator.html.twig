<div class="tab-pane fade"
     id="mediator">
    <h4>Mediaciones en las que ha participado como mediadora/or</h4>
    {% for mediation in mediations_mediator %}
    <div class="alert  {{ cycle(['alert-info', 'alert-success','alert-warning','alert-danger'], loop.index0) }}"><a href="{{ path('mediation_show', {'id': mediation.id}) }}">Mediación de {{ mediation.getFirstStudent }} con {{ mediation.getSecondStudent }}</a> ({{ mediation.getDate|localizeddate('long','none','es') }})
    </div>
    {% endfor %}

</div>