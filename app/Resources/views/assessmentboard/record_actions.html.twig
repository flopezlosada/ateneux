<ul class="list-inline">
    {% if (is_granted("ROLE_TEACHER") and app.user.getTeacher.getMentorCourse.id ==entity.getCourse.getId) or is_granted ("ROLE_ADMIN") %}
        <li><a href="{{ path('meeting_edit', {'id': entity.id}) }}">Editar</a></li>
        <li>
            {{ form_start(delete_form) }}
            <input type="submit" value="{{"Delete"|trans}}">
            {{ form_end(delete_form) }}
        </li>

    {% endif %}
</ul>